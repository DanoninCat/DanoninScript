-- Re Adventures V2 - Macros defaults

return {
    recorder = {
        snapshotInterval = 1,
        maxSnapshots = 7200,
    },

    library = {
        autoSelectNewest = true,
        autoSaveStoppedRecording = true,
    },

    export = {
        schema = "re-adventures-macro",
        version = 2,
    },
}
